import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Index=()=>{
    return (
        <SafeAreaView>
            <Text className="text-blue-500 h-40 w-40 bg-yellow-400">index</Text>
        </SafeAreaView>
    )
}
export default Index;